<Page
    x:Class="LibreSpotUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LibreSpotUWP.Controls"
    xmlns:converters="using:LibreSpotUWP.Converters"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.Resources>
        <Flyout x:Name="SearchFlyout" Placement="Right">
            <TextBox
                x:Name="FlyoutSearchBox"
                Width="200"
                KeyDown="FlyoutSearchBox_KeyDown"
                PlaceholderText="Search"
                TextChanged="FlyoutSearchBox_TextChanged" />
        </Flyout>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <SplitView
            x:Name="RootSplitView"
            Grid.Row="0"
            CompactPaneLength="48"
            DisplayMode="CompactOverlay"
            IsPaneOpen="False"
            OpenPaneLength="200">

            <SplitView.Pane>
                <Grid x:Name="NavigationPaneRoot" Background="Transparent">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0">
                        <Button
                            x:Name="HamburgerButton"
                            Width="48"
                            Height="48"
                            Background="Transparent"
                            Click="HamburgerButton_Click">
                            <TextBlock
                                HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="16"
                                Text="&#xE700;" />
                        </Button>

                        <Button
                            x:Name="SearchIconButton"
                            Width="48"
                            Height="48"
                            Background="Transparent"
                            Click="SearchIconButton_Click"
                            Visibility="Collapsed">
                            <TextBlock
                                FontFamily="Segoe MDL2 Assets"
                                FontSize="18"
                                Text="&#xE721;" />
                        </Button>

                        <TextBox
                            x:Name="SearchBox"
                            Margin="8,0,8,8"
                            HorizontalAlignment="Stretch"
                            KeyDown="SearchBox_KeyDown"
                            PlaceholderText="Search"
                            TextChanged="SearchBox_TextChanged" />

                        <ListBox
                            x:Name="NavListBox"
                            Background="Transparent"
                            SelectionChanged="NavListBox_SelectionChanged">
                            <ListBoxItem Tag="Home" Tapped="NavItem_Tapped">
                                <StackPanel Padding="2" Orientation="Horizontal">
                                    <TextBlock
                                        VerticalAlignment="Center"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="18"
                                        Text="&#xE80F;" />
                                    <TextBlock
                                        Margin="16,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="Home" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="LikedSongs" Tapped="NavItem_Tapped">
                                <StackPanel Padding="2" Orientation="Horizontal">
                                    <FontIcon
                                        VerticalAlignment="Center"
                                        FontFamily="Segoe MDL2 Assets"
                                        Glyph="&#xEB52;" />
                                    <TextBlock
                                        Margin="16,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="Liked Songs" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="Player" Tapped="NavItem_Tapped">
                                <StackPanel Padding="2" Orientation="Horizontal">
                                    <SymbolIcon x:Name="PlayPauseIcon" Symbol="MusicInfo" />
                                    <TextBlock
                                        Margin="16,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="Currently Playing" />
                                </StackPanel>
                            </ListBoxItem>
                            <ListBoxItem Tag="Lyrics" Tapped="NavItem_Tapped">
                                <StackPanel Padding="2" Orientation="Horizontal">
                                    <SymbolIcon Symbol="Microphone" />
                                    <TextBlock
                                        Margin="16,0,0,0"
                                        VerticalAlignment="Center"
                                        Text="Lyrics" />
                                </StackPanel>
                            </ListBoxItem>
                        </ListBox>
                    </StackPanel>

                    <Grid
                        x:Name="CacheStatusPanel"
                        Grid.Row="2"
                        Margin="0,0,8,0"
                        ColumnSpacing="8"
                        Visibility="Collapsed">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <Button
                            x:Name="CacheRefreshButton"
                            Grid.Column="0"
                            Width="48"
                            Height="48"
                            Background="Transparent"
                            Click="CacheRefreshButton_Click">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE72C;" />
                        </Button>

                        <StackPanel
                            Grid.Column="2"
                            VerticalAlignment="Center"
                            Orientation="Horizontal"
                            Spacing="6">
                            <TextBlock
                                x:Name="CacheStatusText"
                                VerticalAlignment="Center"
                                Foreground="{ThemeResource SystemAccentColor}"
                                Tapped="CacheStatusText_Tapped"
                                Text="Cached Content" />
                            <FontIcon
                                x:Name="CacheStatusIcon"
                                VerticalAlignment="Center"
                                FontFamily="Segoe MDL2 Assets"
                                Foreground="{ThemeResource SystemAccentColor}"
                                Glyph="&#xE946;" />
                        </StackPanel>
                    </Grid>

                    <ListBox
                        x:Name="BottomNavListBox"
                        Grid.Row="3"
                        VerticalAlignment="Bottom"
                        Background="Transparent"
                        SelectionChanged="NavListBox_SelectionChanged">
                        <ListBoxItem
                            x:Name="AccountItem"
                            Tag="Account"
                            Tapped="NavItem_Tapped">
                            <FlyoutBase.AttachedFlyout>
                                <Flyout x:Name="AccountFlyout">
                                    <controls:SpotifyAccountControl
                                        x:Name="HeaderAccountControl"
                                        LoadingStateChanged="HeaderAccountControl_LoadingStateChanged"
                                        UserChanged="HeaderAccountControl_UserChanged" />
                                </Flyout>
                            </FlyoutBase.AttachedFlyout>

                            <StackPanel Padding="2" Orientation="Horizontal">
                                <Grid Width="20" Height="20">
                                    <TextBlock
                                        x:Name="DefaultAccountIcon"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        FontFamily="Segoe MDL2 Assets"
                                        FontSize="18"
                                        Text="&#xE77B;" />

                                    <Ellipse x:Name="AccountProfileEllipse" Visibility="Collapsed">
                                        <Ellipse.Fill>
                                            <ImageBrush x:Name="AccountProfileBrush" Stretch="UniformToFill" />
                                        </Ellipse.Fill>
                                    </Ellipse>

                                    <ProgressRing
                                        x:Name="AccountLoadingRing"
                                        Width="16"
                                        Height="16"
                                        Foreground="{ThemeResource SystemControlHighlightAccentBrush}"
                                        IsActive="False" />
                                </Grid>
                                <TextBlock
                                    x:Name="AccountNameText"
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center"
                                    Text="Account" />
                            </StackPanel>
                        </ListBoxItem>
                        <ListBoxItem Tag="Settings" Tapped="NavItem_Tapped">
                            <StackPanel Padding="2" Orientation="Horizontal">
                                <TextBlock
                                    VerticalAlignment="Center"
                                    FontFamily="Segoe MDL2 Assets"
                                    FontSize="18"
                                    Text="&#xE713;" />
                                <TextBlock
                                    Margin="16,0,0,0"
                                    VerticalAlignment="Center"
                                    Text="Settings" />
                            </StackPanel>
                        </ListBoxItem>
                    </ListBox>
                </Grid>
            </SplitView.Pane>

            <SplitView.Content>
                <Frame x:Name="ContentFrame" />
            </SplitView.Content>
        </SplitView>

        <Grid x:Name="MediaBarHost" Grid.Row="1" Background="{ThemeResource SystemControlBackgroundChromeMediumLowBrush}">
            <controls:MediaControllerBar x:Name="WideMediaBar" Visibility="Collapsed" />
            <controls:NarrowMediaControllerBar x:Name="NarrowMediaBar" Visibility="Collapsed" />
        </Grid>

        <Frame
            x:Name="PlayerOverlay"
            Grid.RowSpan="2"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Visibility="Collapsed" />
    </Grid>
</Page>
