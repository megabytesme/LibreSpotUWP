<Page
    x:Class="LibreSpotUWP.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:LibreSpotUWP.Converters"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.Resources>
        <converters:TimeConverter x:Key="TimeConverter" />
        <converters:BoolToVisibility x:Key="BoolToVisibility" />
        <converters:InverseBoolToVisibility x:Key="InverseBoolToVisibility" />
        <converters:VolumeToPercentConverter x:Key="VolumeToPercentConverter" />
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0" Padding="24">
            <StackPanel MaxWidth="800" HorizontalAlignment="Left">
                <TextBlock
                    Margin="0,0,0,20"
                    Style="{StaticResource HeaderTextBlockStyle}"
                    Text="Spotify" />

                <StackPanel>
                    <TextBlock
                        Margin="0,0,0,8"
                        FontWeight="SemiBold"
                        Style="{StaticResource BaseTextBlockStyle}"
                        Text="Stream Configuration" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox
                            x:Name="TrackUriTextBox"
                            Margin="0,0,12,0"
                            PlaceholderText="Enter Spotify URI"
                            Text="spotify:track:3AJwUDP919kvQ9QcozQPxg" />
                        <Button
                            Grid.Column="1"
                            Width="100"
                            Click="PlayButton_Click"
                            Content="Load"
                            Style="{StaticResource AccentButtonStyle}" />
                    </Grid>
                </StackPanel>

                <StackPanel Margin="0,20,0,0">
                    <TextBlock
                        x:Name="LibrespotStatusText"
                        Margin="0,0,0,4"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}" />
                    <TextBlock
                        x:Name="SpotifyApiStatusText"
                        Margin="0,0,0,4"
                        Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                        Style="{StaticResource CaptionTextBlockStyle}" />
                    <Button
                        x:Name="AccountButton"
                        Margin="0,10,0,0"
                        Click="AccountButton_Click"
                        Content="Manage Account" />
                </StackPanel>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Spacing="12">

            <TextBlock FontSize="20" Text="{Binding Track.Name}" />
            <TextBlock FontSize="14" Text="{Binding Track.Artist}" />

            <Image
                x:Name="AlbumArtImage"
                Width="200"
                Height="200"
                Stretch="UniformToFill" />

            <Slider
                x:Name="PositionSlider"
                Maximum="{Binding DurationMs}"
                Minimum="0"
                PointerCaptureLost="PositionSlider_PointerCaptureLost"
                PointerPressed="PositionSlider_PointerPressed"
                Value="{Binding PositionMs, Mode=TwoWay}" />

            <StackPanel Orientation="Horizontal" Spacing="10">
                <TextBlock Text="{Binding PositionMs, Converter={StaticResource TimeConverter}}" />
                <TextBlock Text="/" />
                <TextBlock Text="{Binding DurationMs, Converter={StaticResource TimeConverter}}" />
            </StackPanel>

            <StackPanel Orientation="Horizontal" Spacing="10">
                <Button Click="PrevButton_Click" Content="Prev" />
                <Button
                    Click="ResumeButton_Click"
                    Content="Play"
                    Visibility="{Binding IsPlaying, Converter={StaticResource InverseBoolToVisibility}}" />
                <Button
                    Click="PauseButton_Click"
                    Content="Pause"
                    Visibility="{Binding IsPlaying, Converter={StaticResource BoolToVisibility}}" />
                <Button Click="StopButton_Click" Content="Stop" />
                <Button Click="NextButton_Click" Content="Next" />
            </StackPanel>

            <!--  Volume  -->
            <Slider
                x:Name="VolumeSlider"
                Maximum="100"
                Minimum="0"
                ValueChanged="VolumeSlider_ValueChanged"
                Value="{Binding Volume, Converter={StaticResource VolumeToPercentConverter}}" />

        </StackPanel>

        <Grid
            x:Name="LoadingOverlay"
            Grid.RowSpan="2"
            Background="{ThemeResource SystemControlBackgroundBaseLowBrush}"
            Visibility="Collapsed">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressRing
                    x:Name="LoadingProgressRing"
                    Width="60"
                    Height="60"
                    Margin="0,0,0,12"
                    IsActive="False" />
                <TextBlock
                    HorizontalAlignment="Center"
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="Processing..." />
            </StackPanel>
        </Grid>
    </Grid>
</Page>