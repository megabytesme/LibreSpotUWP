<UserControl
    x:Class="LibreSpotUWP.Controls.NarrowMediaControllerBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="using:LibreSpotUWP.Converters">

    <UserControl.Resources>
        <converters:TimeConverter x:Key="TimeConverter" />
    </UserControl.Resources>

    <Grid
        x:Name="RootGrid"
        Background="{ThemeResource MediaBarBackgroundBrush}"
        ManipulationCompleted="Root_ManipulationCompleted"
        ManipulationDelta="Root_ManipulationDelta"
        ManipulationMode="TranslateX"
        ManipulationStarted="Root_ManipulationStarted"
        Tapped="Root_Tapped">
        <Grid.RowDefinitions>
            <RowDefinition Height="4" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="40" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="48" />
        </Grid.ColumnDefinitions>

        <Border
            x:Name="ProgressFill"
            Grid.Row="0"
            Grid.ColumnSpan="3"
            Height="4"
            HorizontalAlignment="Left"
            Background="{ThemeResource SystemAccentColor}" />

        <Grid
            Grid.Row="1"
            Grid.ColumnSpan="3"
            Padding="8,4,8,8">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="63" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="48" />
                <ColumnDefinition Width="48" />
            </Grid.ColumnDefinitions>

            <Image
                x:Name="AlbumArt"
                Grid.Column="0"
                Width="55"
                Height="55"
                Stretch="UniformToFill" />

            <TextBlock
                x:Name="TrackTitle"
                Grid.Column="1"
                Margin="8,0,0,0"
                FontSize="14"
                FontWeight="SemiBold"
                MaxLines="1"
                TextTrimming="CharacterEllipsis" />

            <Button
                x:Name="TrackArtistButton"
                Grid.Column="1"
                Margin="8,18,0,0"
                Padding="0"
                HorizontalAlignment="Left"
                Background="Transparent"
                BorderThickness="0"
                Click="TrackArtistButton_Click">
                <TextBlock
                    x:Name="TrackArtist"
                    FontSize="12"
                    Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                    MaxLines="1"
                    TextTrimming="CharacterEllipsis" />
            </Button>

            <Button
                x:Name="PlayPauseButton"
                Grid.Column="3"
                Width="45"
                Height="45"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="PlayPauseButton_Click"
                Tapped="PlayPauseButton_Tapped">
                <FontIcon
                    x:Name="PlayPauseIcon"
                    FontFamily="Segoe MDL2 Assets"
                    Glyph="&#xE768;" />
            </Button>

            <Button
                x:Name="PersistButton"
                Grid.Column="2"
                Width="45"
                Height="45"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                Click="PersistButton_Click"
                Tapped="PersistButton_Tapped">
                <FontIcon
                    x:Name="PersistIcon"
                    FontFamily="Segoe MDL2 Assets"
                    FontSize="14"
                    Glyph="&#xE710;" />
            </Button>
            <ProgressRing
                x:Name="PersistProgressRing"
                Grid.Column="2"
                Width="18"
                Height="18"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                IsActive="False"
                Visibility="Collapsed" />

        </Grid>
    </Grid>
</UserControl>
