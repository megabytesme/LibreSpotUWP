<UserControl
    x:Class="LibreSpotUWP.Controls.AlbumGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LibreSpotUWP.Controls">

    <Grid>
        <ItemsControl x:Name="AlbumItems">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <controls:UniformWrapPanel ItemMargin="12" ItemWidth="140" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button
                        Padding="0"
                        HorizontalContentAlignment="Stretch"
                        VerticalContentAlignment="Stretch"
                        Background="Transparent"
                        BorderThickness="0"
                        Click="Album_Click"
                        Tag="{Binding Id}">
                        <StackPanel>
                            <Image
                                Width="140"
                                Height="140"
                                Source="{Binding Images[0].Url}"
                                Stretch="UniformToFill" />

                            <TextBlock
                                Margin="0,4,0,0"
                                FontWeight="SemiBold"
                                Text="{Binding Name}"
                                TextWrapping="Wrap" />

                            <TextBlock Tag="{Binding Artists[0].Id}" TextWrapping="Wrap">
                                <Hyperlink Click="Artist_Click">
                                    <Run Text="{Binding Artists[0].Name}" />
                                </Hyperlink>
                            </TextBlock>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Grid>
</UserControl>