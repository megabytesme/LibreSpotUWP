<UserControl
    x:Class="LibreSpotUWP.Controls.ArtistGridControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LibreSpotUWP.Controls">

    <ItemsControl x:Name="ArtistRepeater">
        <ItemsControl.ItemsPanel>
            <ItemsPanelTemplate>
                <controls:UniformWrapPanel ItemWidth="140" />
            </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>

        <ItemsControl.ItemTemplate>
            <DataTemplate>
                <StackPanel Width="140" Tapped="OnArtistTapped">
                    <Image
                        Width="140"
                        Height="140"
                        Source="{Binding ImageUrl}"
                        Stretch="UniformToFill" />
                    <TextBlock
                        Margin="0,8,0,0"
                        HorizontalAlignment="Center"
                        Text="{Binding Name}"
                        TextTrimming="WordEllipsis"
                        TextWrapping="Wrap" />
                </StackPanel>
            </DataTemplate>
        </ItemsControl.ItemTemplate>
    </ItemsControl>
</UserControl>
