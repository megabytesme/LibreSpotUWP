<Page
    x:Class="LibreSpotUWP.Views.LikedSongsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:LibreSpotUWP.Controls"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock
                Margin="0,0,0,8"
                FontSize="34"
                FontWeight="SemiBold"
                Text="Liked Songs" />
            <TextBlock
                x:Name="SummaryText"
                Margin="0,0,0,8"
                Foreground="{ThemeResource SystemControlForegroundBaseMediumBrush}"
                Text="Your saved tracks in one place." />
            <ComboBox
                x:Name="SortComboBox"
                Width="220"
                SelectionChanged="SortComboBox_SelectionChanged">
                <ComboBoxItem Content="Newest first" Tag="desc" />
                <ComboBoxItem Content="Oldest first" Tag="asc" />
            </ComboBox>
        </StackPanel>

        <StackPanel
            x:Name="CacheIndicator"
            Grid.Row="1"
            Margin="0,0,0,16"
            Orientation="Horizontal"
            Visibility="Collapsed">
            <FontIcon
                Foreground="{ThemeResource SystemAccentColor}"
                FontFamily="Segoe MDL2 Assets"
                Glyph="&#xE946;" />
            <TextBlock
                x:Name="CacheIndicatorText"
                Margin="0,0,8,0"
                Foreground="{ThemeResource SystemAccentColor}"
                Text="Cached" />
            <Button
                x:Name="RefreshButton"
                Width="28"
                Height="28"
                Background="Transparent"
                Click="RefreshButton_Click">
                <FontIcon
                    Foreground="{ThemeResource SystemAccentColor}"
                    FontFamily="Segoe MDL2 Assets"
                    Glyph="&#xE72C;" />
            </Button>
        </StackPanel>

        <controls:TrackListControl x:Name="TrackList" Grid.Row="2" />

        <Grid
            x:Name="LoadingOverlay"
            Grid.RowSpan="3"
            Background="#66000000"
            Visibility="Collapsed">
            <StackPanel
                HorizontalAlignment="Center"
                VerticalAlignment="Center">
                <ProgressRing
                    Width="40"
                    Height="40"
                    Margin="0,0,0,12"
                    IsActive="True" />
                <TextBlock
                    x:Name="LoadingText"
                    HorizontalAlignment="Center"
                    FontSize="16"
                    Text="Loading liked songs..." />
            </StackPanel>
        </Grid>
    </Grid>
</Page>
